# 🚀 Руководство по развертыванию

## Варианты развертывания

### 1. Vercel (Рекомендуется для быстрого старта)

#### Автоматическое развертывание

1. **Подключите репозиторий к Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Нажмите "Add New Project"
   - Импортируйте репозиторий `Jokersochi/primerochnaya`

2. **Настройте переменные окружения:**
   - В настройках проекта добавьте:
   ```
   VITE_GEMINI_API_KEY=ваш_ключ_api
   ```

3. **Деплой произойдет автоматически!**
   - При каждом push в `main` приложение будет автоматически обновляться

#### Ручное развертывание через CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel --prod
```

### 2. Docker + VPS

#### Локальная сборка и тестирование

```bash
# Сборка образа
docker build -t primerochnaya .

# Запуск контейнера
docker run -p 3000:80 primerochnaya
```

Приложение будет доступно на `http://localhost:3000`

#### Развертывание на VPS

```bash
# На вашем VPS сервере

# 1. Клонируйте репозиторий
git clone https://github.com/Jokersochi/primerochnaya.git
cd primerochnaya

# 2. Создайте .env файл
echo "VITE_GEMINI_API_KEY=ваш_ключ" > .env

# 3. Запустите с Docker Compose
docker-compose up -d
```

### 3. Netlify

1. **Подключите репозиторий к Netlify**
2. **Настройки сборки:**
   - Build command: `pnpm install && pnpm run build`
   - Publish directory: `dist`
3. **Добавьте переменную окружения:**
   ```
   VITE_GEMINI_API_KEY=ваш_ключ_api
   ```

### 4. GitHub Pages (Статический хостинг)

```bash
# Установите gh-pages
pnpm add -D gh-pages

# Добавьте в package.json:
"scripts": {
  "predeploy": "pnpm run build",
  "deploy": "gh-pages -d dist"
}

# Деплой
pnpm run deploy
```

## Настройка CI/CD

### GitHub Actions (уже настроено)

В репозитории уже есть `.github/workflows/deploy.yml`. Для активации:

1. **Добавьте секреты в Settings → Secrets → Actions:**
   - `VITE_GEMINI_API_KEY` - ваш Gemini API ключ
   - `VERCEL_TOKEN` - токен Vercel (если используете Vercel)
   - `VERCEL_ORG_ID` - ID организации Vercel
   - `VERCEL_PROJECT_ID` - ID проекта Vercel

2. **Деплой будет автоматически запускаться при push в main**

## Получение Gemini API ключа

1. Перейдите на [Google AI Studio](https://aistudio.google.com/apikey)
2. Создайте новый API ключ
3. Скопируйте и сохраните в безопасном месте

## Мониторинг и аналитика

### Рекомендуемые инструменты:

- **Vercel Analytics** (встроенная аналитика)
- **Google Analytics** - добавьте tracking ID в `index.html`
- **Sentry** - для отслеживания ошибок
- **Plausible/Umami** - приватная аналитика

## Оптимизация производительности

### Уже реализовано:
- ✅ Code splitting
- ✅ Lazy loading компонентов
- ✅ Image optimization
- ✅ Gzip compression
- ✅ Browser caching

### Дополнительные улучшения:

1. **CDN для статики:**
   ```bash
   # Vercel автоматически использует CDN
   # Для других хостингов рассмотрите Cloudflare
   ```

2. **Service Worker для PWA:**
   ```bash
   pnpm add -D vite-plugin-pwa
   ```

## Домен и SSL

### Vercel/Netlify
- SSL сертификаты настраиваются автоматически
- Подключите custom domain в настройках проекта

### VPS с Docker
```bash
# Установите Nginx Proxy Manager или Traefik
# Для автоматических SSL сертификатов от Let's Encrypt

docker run -d -p 80:80 -p 443:443 \
  -v /path/to/data:/data \
  jc21/nginx-proxy-manager:latest
```

## Масштабирование

### Для высоких нагрузок:

1. **Load Balancing** - используйте несколько инстансов
2. **CDN** - Cloudflare для кэширования
3. **Rate Limiting** - ограничьте запросы к Gemini API
4. **Caching** - кэшируйте сгенерированные изображения

## Стоимость запуска

| Платформа | Бесплатный тариф | Платный от |
|-----------|------------------|------------|
| Vercel | Да (Hobby) | $20/мес |
| Netlify | Да | $19/мес |
| GitHub Pages | Да | - |
| VPS (DigitalOcean) | - | $6/мес |
| Gemini API | Бесплатно до 1500 запросов/день | По использованию |

## Готовые команды для быстрого старта

```bash
# Вариант 1: Vercel (самый быстрый)
npx vercel --prod

# Вариант 2: Docker на VPS
git clone https://github.com/Jokersochi/primerochnaya.git
cd primerochnaya
echo "VITE_GEMINI_API_KEY=ваш_ключ" > .env
docker-compose up -d

# Вариант 3: Netlify
netlify deploy --prod --dir=dist
```

---

**Статус:** ✅ Готово к production развертыванию

**Live Demo:** После деплоя ваше приложение будет доступно по адресу, предоставленному хостинг-провайдером.
